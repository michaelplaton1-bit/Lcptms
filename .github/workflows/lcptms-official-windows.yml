name: LCPTMS Posted Window Comparison (Archived)

on:
  workflow_dispatch:

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived. LCPTMS now predicts boarding windows independently."
